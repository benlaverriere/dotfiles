File that are used to produce others, like raw `.terminfo` files that get compiled into `$HOME/.terminfo`. In the future
all of the modules that get `stow`ed could move into a subdirectory for clarity.
